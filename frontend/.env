# Relative URL — Vite proxy forwards /api/* to the backend on the same machine.
# This works on ANY computer without changing IPs.
VITE_API_BASE_URL=https://candidate-reliance-hart-beautifully.trycloudflare.com/api/v1
