# Works for BOTH local dev (Vite proxy) and production (Netlify proxy redirect)
# Never change this — set BACKEND_URL in Netlify dashboard instead
VITE_API_BASE_URL=https://timber-alot-potatoes-tiny.trycloudflare.com/api/v1
