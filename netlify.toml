[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA routing — redirects all routes to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
